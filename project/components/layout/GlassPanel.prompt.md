Surface en verre dosé (recette v3) : voile translucide, reflet d'arête, ombre douce. La brique de base des compositions.

```jsx
<GlassPanel padding="lg">…</GlassPanel>
<GlassPanel strong radius="2xl" padding="xl">…</GlassPanel>
```

Props : `padding` (none/sm/md/lg/xl), `radius` (md/lg/xl/2xl), `strong`, `as`.
